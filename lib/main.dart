import 'package:flutter/material.dart';

void main() => runApp(BookTracker());

class BookTracker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text("ok"),
        ),
      ),
    );
  }
}
